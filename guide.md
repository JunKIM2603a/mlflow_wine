(1) 2025-02-final-homework.pdf의 요구사항(Quality 예측, 이상치 제거, MLflow 등)을 기반으로 분석 목표를 재확인하고, 파이썬 파일의 전체적인 구조를 "인공지능 모델 개발 프로세스" 순서중심의 설명형으로 설계한다.
(2) 심화 EDA 수행: Histogram, KDE, Boxplot, Violin plot, Pairplot 등 다양한 시각화 기법을 활용하여 단변량 및 다변량 분포를 분석하고, 각 기법의 통계적 원리와 시각화 목적을 주석과 마크다운에 상세히 기술한다.
(3) 이상치 탐지 및 처리: Boxplot(IQR), Z-score, Isolation Forest 등 다양한 이상치 탐지 기법의 작동 원리를 설명하고, 각 방법을 적용한 후 데이터 분포 변화를 시각화하여 최적의 제거 전략을 선정한다.
(4) 피처 엔지니어링 및 복합 변수 검증: 도메인 지식을 활용해 변수들을 결합(예: 산도 비율, 밀도/알코올 등)하여 파생 변수를 생성하고, Quality와의 상관관계 및 시각화 분석을 통해 해당 변수의 유효성을 검증하여 최종 Feature Set을 결정한다.
(5) 전처리 파이프라인 구축: Standard/MinMax Scaler와 같은 정규화 기법과 SMOTE 등의 오버샘플링 기법의 이론적 배경을 설명하고, 회귀 및 분류 모델별로 적합한 전처리 시퀀스를 구현한다.
(6) 모델링 및 MLflow 실험: ElasticNet, Random Forest, XGBoost 등 다양한 모델의 알고리즘 원리를 주석과 마크다운으로 설명하고, MLflow를 통해 하이퍼파라미터 튜닝 및 성능 지표를 체계적으로 로깅하는 코드를 작성한다.
(7) 결과 분석 및 문서화: 각 코드 셀 실행 후 출력된 결과값과 그래프에 대해 통계적/직관적 해석을 주석과 마크다운으로 상세히 작성하여, 분석의 논리적 흐름과 결론이 명확히 드러나도록 노트북을 완성한다.
(8) 모델 배포 및 사용: 최고 성능의 모델을 저장하고, wine-quality_2 - wine-quality_2.csv 파일의 첫번째 열이 4001~4897인 데이터를 테스트 데이터로 사용한다.

* wine_data_homework.csv 는 데이터를 로드하고, 첫 번째 열은 인덱스이므로 제거한다.

개요
    분석 배경 및 목표
    분석 전략
데이터 로드 및 초기 분리
    데이터 로드 및 구조 확인
    Train/Test 데이터 분리 (Stratified Split)

탐색적 데이터 분석 (EDA) - Train Set Only
    데이터 파편화 여부 확인 -> 데이터 통합
    데이터 결측 여부 확인 -> 결측치 제거/대체/예측
    범주형 변수 확인 -> 범주형 변수 처리
    라벨 문자 여부 확인 -> 라벨 인코딩
    타겟 분포 확인
    피처별 분포 시각화
전처리 및 피처 엔지니어링
    특징과 라벨 간 상관 관계 확인 -> 신규 특징 추가
    이상치 확인 -> 이상치 제거/치환: 상황/모델에 따라
    특징 간 상관 관계 확인 -> 특징 간 상관성 제거: 상황/모델에 따라
    왜도 확인 -> 변수 치우침 제거
    특징 간 스케일 차이 확인 -> 스케일링: 상황/모델에 따라
    클래스 불균형 확인-> 거리 기반 재샘플링 및 비용민감 모델 고려
    데이터 크기 확인 -> 특징 선택 & 모델 목록 정의
파라미터 그리드 설계 + MLflow
    이상치 제거 파라미터 : IQR 기준치, DBSCAN : 상황/모델에 따라
    재샘플링 모델 및 파라미터 : SMOTE , NearMiss, Other : 상황/모델에 따라
    특징 선택 파라미터 : 특징 개수 : 상황/모델에 따라
    모델 및 모델 파라미터 :
    -Linear Regression : 상황/모델에 따라
    -Lasso/Ridge : 상황/모델에 따라
    -Neural network : 상황/모델에 따라
    -SVM : 상황/모델에 따라
    -Decision Tree : 상황/모델에 따라
    -Random Forest : 상황/모델에 따라
    - ...
    모델별 하이퍼파라미터 그리드 설계
    GridSearchCV
    모델 학습
최종모델 성능 평가
    모델 평가 Quadratic Weighted Kappa (QWK), ... 다양한 방법 사용
    모델 선정
    모델 선정 보고서
    결론
    모델 저장
모델 배포
    모델 사용
